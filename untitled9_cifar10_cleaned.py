# -*- coding: utf-8 -*-
"""Untitled9_CIFAR10_CLEANED.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1QIvNspmPYfAFRvd-E25JGTSAXPuQHQoU
"""

#!git clone https://github.com/qhfan/RMT.git

#!pip install torch torchvision
#!pip install timm

# Commented out IPython magic to ensure Python compatibility.
# %cd RMT/classfication_release

import sys
sys.path.append('.')

#!pip3 install fvcore
#!pip3 install timm==0.9.10
#!pip3 install scipy
#!pip3 install einops
#!pip3 install scikit-learn
#!pip3 install matplotlib

# Instead of:
# from RMT import RMT

# Try:
import torch
import torchvision.transforms as transforms
import torchvision.datasets as datasets
import timm
# The original code tried to import from 'RMT.classfication_release.RMT'
# which is incorrect since 'RMT' is a directory, not a module
# Change to the below line:
from RMT.classfication_release.RMT import RMT_T3  # Correct import statement using relative import

from easydict import EasyDict as edict
#from RMT import RMT_T3
import torch

args = edict({
    "model_name": "RMT_T3",
})

# Define the device before using it
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

model = RMT_T3(args).to(device)

import torch.nn as nn
model.head = nn.Linear(model.head.in_features, 10).to(device)
print("✅ Updated head for CIFAR-10:", model.head)

import torch.nn as nn
model.head = nn.Linear(model.head.in_features, 10).to(device)
print("🔁 Updated model head for CIFAR-10:", model.head)

print(model)

from torchvision import datasets, transforms
from torch.utils.data import DataLoader

# CIFAR-10 Transformations
transform_train = transforms.Compose([
    transforms.RandomCrop(32, padding=4),
    transforms.RandomHorizontalFlip(),
    transforms.ColorJitter(0.2, 0.2, 0.2, 0.1),
    transforms.RandomRotation(15),
    transforms.ToTensor(),
    transforms.Normalize((0.4914, 0.4822, 0.4465), (0.2023, 0.1994, 0.2010)),
])

transform_test = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize((0.4914, 0.4822, 0.4465), (0.2023, 0.1994, 0.2010)),
])

# Load CIFAR-10
train_dataset = datasets.CIFAR10(root="./data", train=True, download=True, transform=transform_train)
val_dataset = datasets.CIFAR10(root="./data", train=False, download=True, transform=transform_test)

train_loader = DataLoader(train_dataset, batch_size=64, shuffle=True, num_workers=4)
val_loader = DataLoader(val_dataset, batch_size=64, shuffle=False, num_workers=4)

print("✅ CIFAR-10 datasets loaded successfully!")

import torch.optim as optim
import torch.nn as nn

device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

criterion = nn.CrossEntropyLoss()
optimizer = optim.AdamW(model.parameters(), lr=3e-4, weight_decay=0.05)

import torch
torch.cuda.empty_cache()

import torch.optim as optim
import torch.nn as nn
import gc  # Import the garbage collection module
import torch.cuda.amp as amp # Import the amp module for automatic mixed precision

# ... (Your code for data loading, model, optimizer, etc.) ...

num_epochs = 100
accumulation_steps = 2  # You might need to further reduce this

scaler = amp.GradScaler()
scheduler = torch.optim.lr_scheduler.StepLR(optimizer, step_size=10, gamma=0.5)

for epoch in range(num_epochs):
    scheduler.step()
    model.train()
    total_loss = 0

    for batch_idx, (images, labels) in enumerate(train_loader):
        images, labels = images.to(device), labels.to(device)
        #print("🧪 Labels range:", labels.min().item(), "-", labels.max().item())

        with amp.autocast(enabled=True):
            outputs = model(images)
            loss = criterion(outputs, labels)

        loss = loss / accumulation_steps
        scaler.scale(loss).backward()

        if (batch_idx + 1) % accumulation_steps == 0:
            scaler.step(optimizer)
            scaler.update()
            optimizer.zero_grad()

        total_loss += loss.item()

        if batch_idx % 10 == 0:
            print(f"Epoch [{epoch+1}/{num_epochs}], Step [{batch_idx}/{len(train_loader)}], Loss: {loss.item():.4f}")

        # Enable garbage collection
        gc.collect()
        torch.cuda.empty_cache()

    print(f"Epoch [{epoch+1}/{num_epochs}], Average Loss: {total_loss / len(train_loader):.4f}")

print("Training complete! ✅")

model.eval()
correct = 0
total = 0

with torch.no_grad():
    for images, labels in val_loader:
        images, labels = images.to(device), labels.to(device)
        outputs = model(images)
        _, predicted = torch.max(outputs, 1)
        total += labels.size(0)
        correct += (predicted == labels).sum().item()

accuracy = 100 * correct / total
print(f"Test Accuracy: {accuracy:.2f}%")

import matplotlib.pyplot as plt
import numpy as np
import torchvision # Import torchvision here

classes = train_dataset.classes  # CIFAR-10 class names

def imshow(img):
    img = img * torch.tensor([0.2023, 0.1994, 0.2010]).view(3, 1, 1) + torch.tensor([0.4914, 0.4822, 0.4465]).view(3, 1, 1)  # unnormalize
    npimg = img.numpy()
    plt.imshow(np.transpose(npimg, (1, 2, 0)))
    plt.show()

# Predict a batch
model.eval()
with torch.no_grad():
    dataiter = iter(val_loader)
    images, labels = next(dataiter)
    images = images.to(device)
    outputs = model(images)
    _, preds = torch.max(outputs, 1)

# Show first 4 images and predictions
imshow(torchvision.utils.make_grid(images[:4].cpu())) # Now torchvision is defined
print("Predicted:", [classes[p] for p in preds[:4]])
